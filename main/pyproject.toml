[project]
name = "travel-log"
version = "0.1.0"
description = "Program to track the Journey of someone's travel"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.28.0",
    "torch>=2.0.0",
    "pandas",
    "numpy",
]
